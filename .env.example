# Build-time flags (SvelteKit exposes only PUBLIC_-prefixed vars to the client).
# Copy to .env and rebuild to apply.

# Show the "Sui impact" tab (the throughput/finality model). Off by default.
PUBLIC_SUI_IMPACT=false
